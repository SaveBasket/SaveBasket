# SaveBasket owner authentication / database
OWNER_EMAIL=you@example.com
OWNER_PASSWORD=use-a-long-random-initial-password
SESSION_SECRET=generate-a-long-random-secret
DATABASE_URL=postgresql://user:password@host/db

# Future production source adapters can be added here without changing the customer UI.
# Example: retailer / marketplace API credentials should be stored in Vercel Environment Variables,
# never committed to GitHub.
